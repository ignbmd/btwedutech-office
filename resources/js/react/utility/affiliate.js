export const fetchActivationStatusInputLabel = (isActive) => {
  return isActive ? "Aktif" : "Tidak Aktif";
};

export const fetchWithdrawStatusInputLabel = (isActive) => {
  return isActive ? "Bisa" : "Tidak Bisa";
};

export const fetchPhoneVerificationStatusInputLabel = (isActive) => {
  return isActive ? "Terverifikasi" : "Belum Terverifikasi";
};
